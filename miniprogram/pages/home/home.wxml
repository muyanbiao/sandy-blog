<view class="page">
  <view class="hero">
    <view class="eyebrow">Kid Reading</view>
    <view class="title">英语点读</view>
    <view class="subtitle">点一句，听一句。孩子可以跟读，也可以录下来回放。</view>
  </view>

  <view wx:if="{{loadMessage}}" class="loading">{{loadMessage}}</view>

  <view class="section" wx:if="{{hasClassBooks}}">
    <view class="section-head">
      <view>
        <view class="section-title">一（4）班</view>
        <view class="section-subtitle">课堂资源</view>
      </view>
    </view>
    <block wx:for="{{classBooks}}" wx:key="id">
      <view class="book-card" bindtap="openReader" data-id="{{item.id}}">
        <view class="mark {{item.accentClass}}"></view>
        <view class="book-main">
          <view class="book-title">{{item.title}}</view>
          <view class="book-subtitle">{{item.subtitle}}</view>
          <view class="book-desc">{{item.description}}</view>
          <view class="tags">
            <text>{{item.age}}</text>
            <text>{{item.topic}}</text>
            <text>{{item.level}}</text>
            <text>{{item.pageCountText}}</text>
          </view>
        </view>
        <view class="arrow">›</view>
      </view>
    </block>
  </view>

  <view class="section" wx:if="{{hasBooks}}">
    <view class="section-head">
      <view>
        <view class="section-title">点读资源</view>
        <view class="section-subtitle">启蒙绘本和单词卡</view>
      </view>
    </view>
    <block wx:for="{{books}}" wx:key="id">
      <view class="book-card" bindtap="openReader" data-id="{{item.id}}">
        <view class="mark {{item.accentClass}}"></view>
        <view class="book-main">
          <view class="book-title">{{item.title}}</view>
          <view class="book-subtitle">{{item.subtitle}}</view>
          <view class="book-desc">{{item.description}}</view>
          <view class="tags">
            <text>{{item.age}}</text>
            <text>{{item.topic}}</text>
            <text>{{item.level}}</text>
            <text>{{item.pageCountText}}</text>
          </view>
        </view>
        <view class="arrow">›</view>
      </view>
    </block>
  </view>
</view>
